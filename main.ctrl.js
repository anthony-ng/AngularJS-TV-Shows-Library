// Declared the main controller
// first retrieve the app module, then use the controller function to instantiate a new controller
angular.module('app').controller("mainController", function() {
  var vm = this;
});
